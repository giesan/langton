{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run (golang)",
            "type": "shell",
            "command": "go run .",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        },
        {
            "label": "Build (golang)",
            "type": "shell",
            "command": "go build -o langton",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}